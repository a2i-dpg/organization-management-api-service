variables:

  # app version, changing version will auto deploy the app to current version
  devBuildImageVersion: "2.3.12"

  # always add prefix k- before production image version
  stagingBuildImageVersion: "prod-2.3.10"

  #app build image
  imageRepository: "registry.nise.gov.bd/nise3-organization-management-api-service"
